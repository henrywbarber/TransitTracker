import React from "react";
import { ScrollView, Text } from "react-native";

function Settings() {
    return (
        <ScrollView>
            <Text style={{ fontSize: 20, textAlign: "center", margin: 10 }}>Settings Screen</Text>
        </ScrollView>
    );
}

export default Settings;
